/*
PARAMETERS

Paramaters are like empty storage boxes we can name whatever we want
*/

function pet(animal) {
  console.log(`I have a ${animal} for a pet.`);
}

pet('cat');

function myName(fName, lName) {
  console.log(`Hello, my name is ${fName} ${lName}.`);
}

myName('Kyle', 'Resnik');